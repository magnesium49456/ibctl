# Generated by tools/generate_configs.py — DO NOT EDIT
services:
  ib-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        IBCTL_BUILD_VERSION: ${IBCTL_BUILD_VERSION:-}
    container_name: ibctl-gateway
    restart: unless-stopped
    oom_score_adj: -1000
    environment:
      DISPLAY: :1
      TWS_USERID: ${TWS_USERID}
      TWS_PASSWORD: ${TWS_PASSWORD}
      TWS_USERID_PAPER: ${TWS_USERID_PAPER:-}
      TWS_PASSWORD_PAPER: ${TWS_PASSWORD_PAPER:-}
      TRADING_MODE: ${TRADING_MODE:-paper}
      TWOFACTOR_CODE: ${TWOFACTOR_CODE:-}
      TOTP_PROVIDER: ${TOTP_PROVIDER:-builtin}
      TWOFA_DEVICE: ${TWOFA_DEVICE:-}
      TWOFA_TIMEOUT_ACTION: ${TWOFA_TIMEOUT_ACTION:-restart}
      TWOFA_EXIT_INTERVAL: ${TWOFA_EXIT_INTERVAL:-120}
      RELOGIN_AFTER_TWOFA_TIMEOUT: ${RELOGIN_AFTER_TWOFA_TIMEOUT:-yes}
      IBCTL_OCR_VERIFICATION: ${IBCTL_OCR_VERIFICATION:-auto}
      IBCTL_OCR_VERIFICATION_STRICT: ${IBCTL_OCR_VERIFICATION_STRICT:-false}
      IBCTL_ATSPI_FALLBACK: ${IBCTL_ATSPI_FALLBACK:-auto}
      TWS_ACCEPT_INCOMING: ${TWS_ACCEPT_INCOMING:-accept}
      TWS_TRUSTED_IPS: ${TWS_TRUSTED_IPS:-127.0.0.1}
      TWS_MASTER_CLIENT_ID: ${TWS_MASTER_CLIENT_ID:-}
      READ_ONLY_API: ${READ_ONLY_API:-no}
      BYPASS_WARNING: ${BYPASS_WARNING:-yes}
      ALLOW_BLIND_TRADING: ${ALLOW_BLIND_TRADING:-no}
      TWS_API_INSTRUMENT_TIMEZONE: ${TWS_API_INSTRUMENT_TIMEZONE:-UTC format}
      DISMISS_PASSWORD_EXPIRY_WARNING: ${DISMISS_PASSWORD_EXPIRY_WARNING:-}
      ACCEPT_BID_ASK_LAST_SIZE_DISPLAY_UPDATE_NOTIFICATION: ${ACCEPT_BID_ASK_LAST_SIZE_DISPLAY_UPDATE_NOTIFICATION:-ignore}
      CONFIRM_CRYPTO_CURRENCY_ORDERS: ${CONFIRM_CRYPTO_CURRENCY_ORDERS:-manual}
      AUTO_RESTART_TIME: ${AUTO_RESTART_TIME:-09:05 PM}
      TWS_COLD_RESTART: ${TWS_COLD_RESTART:-}
      SAVE_TWS_SETTINGS_AT: ${SAVE_TWS_SETTINGS_AT:-}
      JAVA_HEAP_SIZE: ${JAVA_HEAP_SIZE:-768}
      IBCTL_LIVE_API_PORT: ${IBCTL_LIVE_API_PORT:-4001}
      IBCTL_PAPER_API_PORT: ${IBCTL_PAPER_API_PORT:-4002}
      IBCTL_LIVE_SOCAT_PORT: ${IBCTL_LIVE_SOCAT_PORT:-4003}
      IBCTL_PAPER_SOCAT_PORT: ${IBCTL_PAPER_SOCAT_PORT:-4004}
      TZ: ${TZ:-America/New_York}
      VNC_SERVER_PASSWORD: ${VNC_SERVER_PASSWORD:-}
      RUST_LOG: ${RUST_LOG:-ibctl=info}
      IBCTL_LOG_DIR: /opt/ibctl/persist/logs
      IBCTL_COMMAND_SERVER_ENABLED: ${IBCTL_COMMAND_SERVER_ENABLED:-true}
      IBCTL_COMMAND_HOST: ${IBCTL_COMMAND_HOST:-127.0.0.1}
      IBCTL_COMMAND_PORT: ${IBCTL_COMMAND_PORT:-7462}
      IBCTL_COMMAND_HOST_PAPER: ${IBCTL_COMMAND_HOST_PAPER:-127.0.0.1}
      IBCTL_COMMAND_PORT_PAPER: ${IBCTL_COMMAND_PORT_PAPER:-7463}
      IBCTL_DASHBOARD_ENABLED: ${IBCTL_DASHBOARD_ENABLED:-true}
      IBCTL_DASHBOARD_PORT: ${IBCTL_DASHBOARD_PORT:-8080}
      IBCTL_DASHBOARD_TOKEN: ${IBCTL_DASHBOARD_TOKEN:-}
      IBCTL_DASHBOARD_AUTH_SECRET: ${IBCTL_DASHBOARD_AUTH_SECRET:-}
      IBCTL_DASHBOARD_EXTERNAL_URL: ${IBCTL_DASHBOARD_EXTERNAL_URL:-}
      IBCTL_ROOT_PATH: ${IBCTL_ROOT_PATH:-}
      IBCTL_DEBUG_MODE: ${IBCTL_DEBUG_MODE:-}
      IBCTL_NOTIFICATIONS_ENABLED: ${IBCTL_NOTIFICATIONS_ENABLED:-}
      IBCTL_NOTIFICATION_CHANNEL: ${IBCTL_NOTIFICATION_CHANNEL:-}
      IBCTL_NTFY_URL: ${IBCTL_NTFY_URL:-}
      IBCTL_NTFY_TOPIC: ${IBCTL_NTFY_TOPIC:-}
      IBCTL_NTFY_TOKEN: ${IBCTL_NTFY_TOKEN:-}
      IBCTL_SLACK_WEBHOOK_URL: ${IBCTL_SLACK_WEBHOOK_URL:-}
      IBCTL_TELEGRAM_BOT_TOKEN: ${IBCTL_TELEGRAM_BOT_TOKEN:-}
      IBCTL_TELEGRAM_CHAT_ID: ${IBCTL_TELEGRAM_CHAT_ID:-}
      IBCTL_NTFY_ACTION_SIGNING_KEY: ${IBCTL_NTFY_ACTION_SIGNING_KEY:-}
      IBCTL_TWOFA_MAX_IMMEDIATE_ATTEMPTS: ${IBCTL_TWOFA_MAX_IMMEDIATE_ATTEMPTS:-}
      IBCTL_TWOFA_ON_TIMEOUT: ${IBCTL_TWOFA_ON_TIMEOUT:-}
      IBCTL_TWOFA_STRATEGY: ${IBCTL_TWOFA_STRATEGY:-}
      IBCTL_TWOFA_INTERVALS_MINUTES: ${IBCTL_TWOFA_INTERVALS_MINUTES:-}
      IBCTL_TWOFA_CALLBACK_VALID_HOURS: ${IBCTL_TWOFA_CALLBACK_VALID_HOURS:-}
      IBCTL_TWOFA_COUNTER_RESET: ${IBCTL_TWOFA_COUNTER_RESET:-}
      IBCTL_TWOFA_STABLE_SECS: ${IBCTL_TWOFA_STABLE_SECS:-}
      IBCTL_TWOFA_COLD_RESTART_PREEMPTS_HITL: ${IBCTL_TWOFA_COLD_RESTART_PREEMPTS_HITL:-}
      IBCTL_TWOFA_NTFY_SEND_RETRIES: ${IBCTL_TWOFA_NTFY_SEND_RETRIES:-}
      IBCTL_API_PORT_PROBE_INTERVAL_SECS: ${IBCTL_API_PORT_PROBE_INTERVAL_SECS:-}
      IBCTL_API_PORT_PROBE_FAILS_BEFORE_REVOKE: ${IBCTL_API_PORT_PROBE_FAILS_BEFORE_REVOKE:-}
      IBCTL_RECOVERY_DISABLED: ${IBCTL_RECOVERY_DISABLED:-}
      IBCTL_RECOVERY_AGGRESSIVE_MAX_SECS: ${IBCTL_RECOVERY_AGGRESSIVE_MAX_SECS:-}
      IBCTL_RECOVERY_BACKOFF_MAX_SECS: ${IBCTL_RECOVERY_BACKOFF_MAX_SECS:-}
      IBCTL_RECOVERY_BACKOFF_INTERVAL_SECS: ${IBCTL_RECOVERY_BACKOFF_INTERVAL_SECS:-}
      IBCTL_RECOVERY_MIN_SUCCESS_DWELL_SECS: ${IBCTL_RECOVERY_MIN_SUCCESS_DWELL_SECS:-}
      IBCTL_RECOVERY_FINGERPRINT_STREAK: ${IBCTL_RECOVERY_FINGERPRINT_STREAK:-}
      IBCTL_RECOVERY_FORCE_RESET: ${IBCTL_RECOVERY_FORCE_RESET:-}
      IBCTL_IB_STATUS_ENABLED: ${IBCTL_IB_STATUS_ENABLED:-}
      IB_STATUS_CHECK_INTERVAL: ${IB_STATUS_CHECK_INTERVAL:-300}
      IB_STATUS_REGION: ${IB_STATUS_REGION:-NA}
      IBCTL_ZMQ_ENABLED: ${IBCTL_ZMQ_ENABLED:-}
      IBCTL_ZMQ_PORT: ${IBCTL_ZMQ_PORT:-5556}
      IBCTL_LOG_LEVEL: ${IBCTL_LOG_LEVEL:-}
      IBCTL_ACCEPT_INCOMING: ${IBCTL_ACCEPT_INCOMING:-}
      IBCTL_SESSION_ACTION: ${IBCTL_SESSION_ACTION:-}
      IBCTL_RESTART_DELAY: ${IBCTL_RESTART_DELAY:-}
      IBCTL_RELOGIN_FAILURE_ACTION: ${IBCTL_RELOGIN_FAILURE_ACTION:-}
      IBCTL_RELOGIN_ATTEMPTS: ${IBCTL_RELOGIN_ATTEMPTS:-}
      IBCTL_LOGIN_TIMEOUT: ${IBCTL_LOGIN_TIMEOUT:-}
      IBCTL_SITE_ROLE: ${IBCTL_SITE_ROLE:-}
      IBCTL_AUTO_LAUNCH: ${IBCTL_AUTO_LAUNCH:-}
      IBCTL_AUTO_UPDATE: ${IBCTL_AUTO_UPDATE:-}
      IBCTL_VERSION: ${IBCTL_VERSION:-latest}
    volumes:
    - ibctl-persist:/opt/ibctl/persist
    ports:
    - ${IBCTL_LIVE_API_BIND:-127.0.0.1}:${IBCTL_LIVE_API_PORT:-4001}:${IBCTL_LIVE_SOCAT_PORT:-4003}
    - ${IBCTL_PAPER_API_BIND:-127.0.0.1}:${IBCTL_PAPER_API_PORT:-4002}:${IBCTL_PAPER_SOCAT_PORT:-4004}
    - ${IBCTL_VNC_BIND:-127.0.0.1}:5900:5900
    - ${IBCTL_VNC_BIND:-127.0.0.1}:6080:6080
    - 127.0.0.1:7462:7462
    - 127.0.0.1:7463:7463
    - ${IBCTL_DASHBOARD_BIND:-127.0.0.1}:${IBCTL_DASHBOARD_PORT:-8080}:${IBCTL_DASHBOARD_PORT:-8080}
    tty: true
volumes:
  ibctl-persist:
